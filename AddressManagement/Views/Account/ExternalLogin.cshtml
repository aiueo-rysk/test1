@model ExternalLoginViewModel
@{
    ViewData["Title"] = "登録";
}

<h2>@ViewData["Title"]</h2>
<h4>あなたの @ViewData["LoginProvider"] アカウントを関連付けます。</h4>
<hr />

<p class="text-info">
    あなたは正常に<strong>@ViewData["LoginProvider"]</strong>アカウントで認証されました。
    このサイトで利用するEメールアドレスを下に入力して[登録]ボタンをクリックし、ログインを終了してください。
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-action="ExternalLoginConfirmation" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Email"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <input type="hidden" name="provider" value="@ViewData["LoginProvider"]" />
            <button type="submit" class="btn btn-default">登録</button>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
